// Returned by every `show_*` tool so the model knows the data is already on the
// user's screen as a widget and answers briefly instead of repeating the table.
export const WIDGET_NOTE =
  "Already on the user's screen as a widget — reply with a one-line takeaway or the specific number asked, do not re-list this.";
